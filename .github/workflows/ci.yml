name: React CI/CD Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Build React App
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      # Install dependencies
      - name: Install Dependencies
        working-directory: ./frontend
        run: npm install

      # Build the project
      - name: Build Project
        working-directory: ./frontend
        run: |
          CI=false npm run build

      # Debug: Check Build Output
      - name: Debug Build Output
        run: |
          ls -la ./frontend
          ls -la ./frontend/build

  deploy:
    name: Deploy React App to EC2
    runs-on: ubuntu-latest
    needs: build

    steps:
      # Checkout the code again (required for the deploy step)
      - name: Checkout Code
        uses: actions/checkout@v3

      # Setup SSH for secure file transfer
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      # Debug Permissions and Environment
      - name: Debug Environment
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_IP }} << 'EOF'
          which pm2 || echo "PM2 not installed"
          ls -ld /var/www || echo "No permissions for /var/www"
          exit
          EOF
      # Debug: Check Build Step Output
      - name: Debug Build Output
        run: |
          ls -la ./frontend
          ls -la ./frontend/build

      # Debug: Verify Current Directory
      - name: Verify Current Directory
        run: pwd

      # Deploy the React app to EC2
      - name: Deploy React App
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_IP }} << 'EOF'
          pm2 stop react-app || true
          rm -rf /var/www/react-app/*
          mkdir -p /var/www/react-app
          exit
          EOF

          scp -o StrictHostKeyChecking=no -r ./frontend/build/* ubuntu@${{ secrets.EC2_IP }}:/var/www/react-app/

          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_IP }} << 'EOF'
          cd /var/www/react-app
          pm2 start serve --name react-app -- -s . -l 3000
          pm2 save
          exit
          EOF
